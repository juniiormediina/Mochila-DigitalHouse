const suma = (n1, n2) => {
  return n1 + n2;
};

const resta = function (n1, n2) {
  return n1 - n2;
};

const multiplicacion = (n1, n2) => {
  return n1 * n2;
};

const division = (n1, n2) => {
  return n1 / n2;
};

console.log(suma(2, 4) + resta(3, 1));
